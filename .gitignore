node_modules/
.next/
tsconfig.tsbuildinfo

# Local env / data
.env
.env.local
data/
drizzle/meta/_journal.json.bak

# Docker Compose override local (dev / smoke tests) — ne doit PAS aller en prod.
docker-compose.override.yml
cookies.txt
